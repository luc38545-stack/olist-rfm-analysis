# 结论与运营建议（Olist 真实数据）

> 数据与口径见 `01_数据体检报告_olist.md`，分层结果见 `02_RFM分层结果与画像.md`。
> 以下四条建议里的数字都与分层汇总表对得上。

---

## 一、四条发现与对应动作

### 发现 1：平台是"大额单次购买"驱动型

- **数字**：20,699 名"重要挽留"客户贡献 40.05% 交易额（6,176,778.83 BRL），加上 14,406 名"重要发展"（28.22%），合计 37.6% 的客户贡献了 68.3% 的交易额。复购客户（F≥2）只有 2,801 人，占 3%，对应金额 5.60%。
- **含义**：业绩靠大额单次交易撑着，复购极度匮乏。
- **动作**（两类客户的运营动作不同，不能混成一个"沉睡池"）：
  - **重要挽留 20,699 人（沉睡大额）→ 唤醒召回**：90 天未回购推送 + 大额券 + 个性化触达。按 5% 唤醒率、客单价 298.41 BRL 估算，约可回收 30.9 万 BRL（20,699 × 5% × 298.41 ≈ 308,839）。这是交易额情景估算，不是 ROI——优惠券成本、触达成本、履约成本都没扣。
  - **重要发展 14,406 人（近期大额首购，不是沉睡）→ 首购后 30/60 天复购培育**：购买后及时跟进（物流确认、品类推荐、限时复购券），目标是把高潜首购转成复购。这类客户不适用沉睡召回的唤醒率假设。

### 发现 2：复购客群规模极小，但价值密度高

- **数字**：重要价值 + 重要保持共 2,238 名复购高额客户，贡献 5.25% 交易额（809,137.21 BRL），观察窗口内人均累计消费 361.5 BRL，约为全部单次客户人均（160.8 BRL）的 2.2 倍。
- **含义**：复购客户的单次客单价（168–171 BRL）低于大额单次客（298–302 BRL），但平均复购约 2.1 单，使观察窗口内的人均累计消费达到 353–373 BRL，从而进入 M 高分位。这批客户数量少，但累计价值较高，应作为重点维护对象。
- **动作**：① VIP 专属客服 + 提前体验权（每月专属促销）；② 裂变激励，介绍好友各得 30 BRL 券，把复购体验嵌进拉新链路。

### 发现 3：沉睡小额客户数量多，但单客价值较低

- **数字**：一般挽留 33,715 人，贡献 15.84% 交易额，人均累计消费 72.5 BRL，约为平台均值 165.2 的 44%。
- **含义**：量大但低价值，一个个去运营性价比太低。
- **动作**：采用月度批量邮件和通用小额券（5–10 BRL）进行低成本触达，减少人工运营投入。若按 2% 唤醒率估算，单轮对应交易额约 4.9 万 BRL（33,715 × 2% × 72.47 ≈ 48,871，未扣成本）。营销预算优先投入发现 1 和 2 中的客群。

### 发现 4：客户数量集中在 SP 州（目前只验证了数量维度）

- **数字**：所有客群的第一大州都是 SP，占比 34%–62%，其余 26 州分散。
- **含义**：SP 的客户数量高度集中，但跨州的复购率、消费金额、履约表现是否同质还没检验，暂时不能下"不用分州运营"的结论。
- **动作**：以巴西州为单位做一次"履约时延 vs 复购"的二级分析（用 `olist_orders` 的 `order_delivered_customer_date - order_purchase_timestamp`），确认区域差异后再决定要不要分州运营。

---

## 二、复现方式

```bash
cd <项目根目录/P1_RFM>
python scripts/rfm_pipeline.py     # 计算 + 控制台对账，输出 output/*.csv 与 3 张 PNG
```

跑完控制台会输出客户数、订单数、金额三项对账结果，不一致会直接报错终止。Power BI 建模用的数据集另跑 `python scripts/export_powerbi_dataset.py`。

---

## 三、假设与局限

1. **参考日 2018-09-01**：数据集尾部 2018 年 9–10 月只有 20 单，看着像导出截断。锚定这个日期能保证 R 可复现，但 R 维度对 2016 年 10 月就购买过的极早期客户偏长，对临近客户区分度好。
2. **金额币种为 BRL（巴西雷亚尔）**：源数据没有汇率，未做换算。
3. **F 的阈值取复购（≥2 单）**：这是数据驱动的决定，不是经典的五分位中位切，原因写在报告里。
4. **数据许可 CC BY-NC-SA 4.0**：非商用、须署名、衍生作品同协议共享。简历和作品集可用，商业产品不能直接套。
5. **RFM 是截面画像**：反映的是观察窗口内的购买行为，不直接等同于客户生命周期价值；召回收益都是交易额情景估算，不是 ROI。
6. **没用到的表**：geolocation（地图）、reviews（评价）、products（品类偏好）都还没做，可作为后续补充。

---

## 四、Streamlit 看板

```bash
cd <项目根目录/P1_RFM>
python -m streamlit run app/streamlit_app.py
```

浏览器打开 `http://localhost:8501`。看板分四段：顶部三个核心指标、方法口径折叠说明、分层汇总表、三张关键图、客群画像表与核心结论。

如果 `127.0.0.1` 打不开，先检查本机有没有配全局 HTTP 代理——需要把 `127.0.0.1,localhost` 加进代理例外，这和项目代码无关。

截图见 `output/shot_dashboard_full.png`。

---

## 五、后续可做的方向

1. **地理二级分析**：按州看履约时延与复购的关系，判断是否需要区域差异化运营；
2. **品类偏好**：用 products 表看不同客群的品类结构，给召回活动选品提供依据；
3. **评价与复购**：用 reviews 表验证低分评价是否显著拉低复购率；
4. **卖家维度**：从卖家侧看大额客户集中在哪些卖家，作为 BD 谈判的依据；
5. **Power BI 看板**：把 `output/powerbi/` 的三张表做成可交互仪表盘，与 Streamlit 版并行。
