# 数据体检报告（Brazilian E-Commerce by Olist）

> 数据源：Brazilian E-Commerce Public Dataset by Olist（Kaggle 官方，真实匿名商业数据，10 万订单，2016–2018）
> 许可：CC BY-NC-SA 4.0（署名、非商用、相同方式共享）
> 获取：官方 GitHub 仓库已下线，改从镜像仓库下载 8 张 CSV，跳过 geolocation 地理表（本项目用不上）
> 明细数据：`01_数据体检_事实_olist.md`（脚本生成）｜日期：2026-09-07

---

## 一、结论摘要

数据质量扎实，可以直接进入 RFM 分析。体检发现三个会影响方法设计的特征：

| # | 发现 | 对设计的影响 |
|---|---|---|
| 1 | 97% 的客户只买过 1 单（90,556 / 93,357） | F 维度天然区分度低，分层阈值要数据驱动调整，不能照搬教科书 |
| 2 | 订单金额右偏严重（9.59 ~ 13,664，均值 160 BRL） | M 打分必须用分位数，不能用等距区间 |
| 3 | 数据集尾部截断，2018-09 之后只有 20 单 | R 的参考日不能锚定最大日期，要锚定数据主体的结束日 |

## 二、数据规模（已导入 SQLite：`data_olist/olist.db`）

| 表 | 行数 | 说明 |
|---|---|---|
| olist_orders | 99,441 | 含 order_status 与 7 个时间戳字段 |
| olist_customers | 99,441 | 去重后 96,096 名客户 |
| olist_payments | 103,886 | 每单 1 到多行（分期 / 多方式） |
| olist_items | 112,650 | 明细，含 price 与 freight |
| olist_reviews | 99,224 | 1–5 分评价 |
| olist_products / sellers / category_translation | 32,951 / 3,095 / 70 | 周边维度表 |

## 三、体检明细

### orders

| 检查项 | 结果 | 判断 |
|---|---|---|
| order_id 唯一 | 99,441 = 99,441 | 通过 |
| 状态分布 | delivered 96,478 / shipped 1,107 / canceled 625 / unavailable 609 / invoiced 314 / processing 301 / 其他 7 | 状态枚举正常 |
| 购买时间范围 | 2016-09-04 ~ 2018-10-17 | 两年窗口 |
| 尾部稀疏 | 2018-09 仅 16 单、10 月 4 单 | 疑似导出截断 |

### 客户与金额

| 检查项 | 结果 | 判断 |
|---|---|---|
| 去重客户 | 96,096（按 customer_unique_id 去重；一人跨地址下单会生成多个 customer_id） | 基本正常 |
| payments 负值 / 缺失 | 0 | 通过 |
| 已交付且有支付记录的订单 | 96,477 单（96,478 单 delivered 中有 1 单无支付记录） | 通过 |
| 有效 RFM 客户基数 | 93,357 | 通过 |
| 金额合计 | 15,422,461.77 BRL | 通过 |
| 人均订单数 | 1 ~ 15，平均 1.03 | 单次购买主导（97%） |

### 可用画像维度

- **州分布**：SP 40,302 居首，其次 RJ 12,384、MG 11,259，地域集中度是真实的；
- **评分分布**：5 分 57,328、1 分 11,424，好评偏差真实存在，可作为补充分析素材。

## 四、口径约定

1. **成交口径**：`order_status = 'delivered'` 且支付表中有记录，共 96,477 单。原始数据有 96,478 单 delivered，其中 1 单没有支付记录，未进入 RFM
2. **金额口径**：对上述 96,477 单的 `olist_payments.payment_value` 先按订单汇总，再计算客户累计金额
3. **客户口径**：以 `customer_unique_id` 作为 RFM 主体（同一人跨地址的多个 customer_id 需合并；join 后无缺失记录）
4. **R 参考日 = 2018-09-01**（数据主体在 2018-08 截断，尾部仅 20 单；锚定截断日能让 R 不失真且可复现）

以上口径下基准为：**96,477 单 / 93,357 名客户 / 15,422,461.77 BRL**。

## 五、对分层设计的调整

97% 的客户只买过一单，意味着 F 维度只有 3% 的客户大于 1 单。如果照搬经典的 R/F/M 五分位八类分层，会有 90% 以上的客户挤在"一般"档，八类结构失去意义。

所以这里改了两处：**F 的高低按是否复购（≥2 单）判定**，而不是按五分位中位数；R 和 M 仍用动态五分位。这样八类分层能真实反映这批数据的结构——绝大多数顾客来过一次就不再来，运营的重点是复购和流失召回。

## 六、复现方式

```bash
cd <项目根目录/P1_RFM>
python scripts/build_olist_db.py     # CSV -> olist.db，并生成体检事实文件
```

用 DBeaver 复跑体检查询：`sql/01_数据体检_olist.sql`。

---

*下一篇：`02_RFM分层结果与画像.md`*
